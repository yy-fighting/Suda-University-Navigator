<view class="route-container">
  <!-- 路线一（可跳转） -->
  <view class="route-card" bindtap="navigateToRoute1">
    <image src="{{bg1}}" mode="aspectFill" class="route-bg"></image>
    <view class="route-title">游览路线一</view>
  </view>
  
  <!-- 其他路线（静态） -->
  <view class="route-card">
    <image src="{{bg2}}" mode="aspectFill" class="route-bg"></image>
    <view class="route-title">游览路线二</view>
  </view>
  
  <view class="route-card">
    <image src="{{bg3}}" mode="aspectFill" class="route-bg"></image>
    <view class="route-title">游览路线三</view>
  </view>
  
  <view class="route-card">
    <image src="{{bg4}}" mode="aspectFill" class="route-bg"></image>
    <view class="route-title">游览路线四</view>
  </view>
</view>